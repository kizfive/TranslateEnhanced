# TranslateEnhanced 今日交接（2026-08-03）

> 适用对象：另一台设备/另一位维护者无缝接手
> 分支：`test`（全部工作都在此分支，**尚未合并 main**）
> 远程：`https://github.com/kizfive/TranslateEnhanced.git`
> 详细架构文档：见同目录 `HANDOVER.md`（已随代码更新）

---

## 0. 一句话状态

插件处于**功能开发期**：今天的 15 个提交全部在 `test` 分支，CI 每次推送都自动构建并已通过；`main` 分支还是旧版，**等设备实测通过后合并**。合并前不要直接改 `main`。

---

## 1. 今天做了什么（按时间线，共 15 个提交）

| 时间 | 提交 | 内容 |
|---|---|---|
| 10:42 | `1def2fc` | 链接保留修复（占位符方案）+ 消息菜单"重新翻译"选项 |
| 10:56 | `e92787d` | 修 `Regex.findAll` 默认参数桥接编译错误 |
| 11:34 | `a0161df` | 设置页重构为 Discord 原生风格（CheckedSetting 单选/开关、UiKit 样式）；译文用 DiscordParser 重渲染（URL 可点击、emoji/提及恢复）；统一日志为单文件 `translate.log` |
| 11:37 | `9ac1e6c` | 修 Material `BOX_BACKGROUND_FILLED` 常量名、builder 返回类型 |
| 12:36 | `33e6e47` | 修 `isNotBlank()` 触发 `d0.d0.b` IntIterator 崩溃（违反 R8 铁律） |
| 14:16 | `bf7189c` | URL 正则按 CJK/全角/emoji 截断 + 尾部标点裁剪（修复"链接吞掉后续文本"） |
| 14:25 | `02421cc` | **URL 策略改为默认不清洗**：链接交给翻译引擎原样保留，翻译后校验补回；占位保护模式保留为选项 |
| 14:49 | `11285c5` | **修复自动翻译完全失效**：126021 的 `WidgetChatList.onNewMessage` 不存在，改 hook `StoreStream.handleMessageCreate(api Message)`；给跳过路径加 debug 日志 |
| 15:05 | `ea1adc5` | 自动翻译覆盖**历史消息/滚动加载**（processMessageText 触发 + 开启时扫描已加载列表） |
| 15:29 | `9386bcd` | **自动翻译批量合并**：1s 窗口、每批 ≤10 条、单条 ≤1500 字符，一次 LLM 请求；修"关闭自动翻译误报失败暂停"toast |
| 15:51 | `de475dd` | 批量进度 toast（开始/请求中/完成，2.5s 限频）；设置项"翻译自己发送的消息" |
| 16:25 | `7c1d027` | 批量输出改**行格式 `[id] 译文`**（修复模型 JSON 未转义引号导致整批失败）；降级 toast 合并进完成提示 |
| 16:30 | `a5b9d8c` | 修批量解析里 `Regex.find`/`split` 默认参数桥接编译错误 |
| 16:54 | `ad56af4` | **关闭自动翻译即停止**（清队列 + 丢弃在途响应，不再降级）；**频道级提示词/术语表**；**持久化翻译缓存** |
| 17:29 | `2be64b3` | 频道配置与缓存管理收进**设置子页面**；自动生成后**自动保存**（修复"生成完再打开是空的"） |

**已放弃的功能**：大模型思考参数（DeepSeek `thinking` / OpenAI `reasoning_effort` 等参数不统一，适配成本高），代码已完全移除。

---

## 2. 项目当前状态

### 2.1 分支与 CI
- 当前分支：`test`（跟踪 `origin/test`），本地干净
- CI：push `test` 自动构建（工作流 `branches-ignore: builds`），产物在 Actions 页 Artifacts 下载；**发布到 `builds` 分支仍只在 main 推送时发生**
- 最近成功构建：run 30801640606（`2be64b3`）
- 交接条件：设备实测通过 → merge `test` → `main` → push（届时 CI 自动发布正式包）

### 2.2 功能清单（当前 test 分支）
- 双后端：Google / LLM（OpenAI 兼容），LLM 失败自动降级 Google（批量模式降级条数汇总进完成 toast）
- 手动翻译（菜单 + `/translate` 命令）、显示原文、**重新翻译**（绕过缓存 + 防回显提示）
- 自动翻译：新消息 + 历史/滚动加载 + 开启时扫描；批量合并 LLM 请求；进度 toast；可翻译自己发的消息；**关闭即停止**
- 链接：默认不清洗（引擎保留 + 校验补回），译文经 DiscordParser 重渲染**可点击**；可选占位保护模式
- emoji/提及/自定义表情/时间戳：清洗后统一还原（占位符 `[[EMOJI_n]]`/`[[TAG_n]]`），重渲染恢复原生显示
- 频道级配置：提示词 + 术语表（`原词=译文`），LLM 自动生成（自动保存），设置子页面管理
- 持久化缓存：`/sdcard/Aliucord/translate_cache.json`（上限 1000 条），命中不再请求后端；可全清/按频道清
- 统一日志：`/sdcard/Aliucord/translate.log`（DEBUG/WARN/ERROR 分级，崩溃无条件写）
- 设置页：Discord 原生风格（CheckedSetting 单选/开关、UiKit 样式、Discord 配色）

### 2.3 关键文件地图
```
TranslateEnhanced/src/main/kotlin/com/aliucord/plugins/translate/
├── Translate.kt            # 插件入口：所有 patch、自动翻译统一入口、菜单
├── TranslateController.kt  # 调度：批量队列/调度器、缓存、降级、停止逻辑
├── TextCleaner.kt          # 清洗+占位符还原（URL/emoji/标记）、URL 截断规则
├── ChannelConfig.kt        # 频道提示词/术语表存储 + 频道登记表
├── ChannelConfigUi.kt      # 频道配置编辑对话框 + 自动生成（共用 UI）
├── ChannelConfigsPage.kt   # 设置子页面：按频道管理配置/缓存
├── TranslationCache.kt     # 持久化翻译缓存（JSON 文件）
├── PluginSettings.kt       # 设置页（Discord 风格）
├── backend/
│   ├── LLMTranslator.kt    # 单条/批量翻译、complete()、行格式解析
│   └── GoogleTranslator.kt
├── auto/                   # AutoTranslateManager / LanguageDetector
├── strings/                # IStrings + Zh/En（改文案必须三处同步）
└── utils/                  # ResourceUtils（toRealString/safeIsBlank/safeGetString ★）、DebugLogger
```

---

## 3. ⚠️ 核心坑（改代码前必读）

### 3.1 R8 混淆与 `d0.d0.b`
Discord 本体 R8 混淆后，`SettingsAPI.getString()` 与 `Message.content` 运行时可能是混淆的 `CharSequence` 包装类（`d0.d0.b`），对其调用 Kotlin String/CharSequence 扩展会崩 `IntIterator`。

**三条铁律：**
1. 外部值（settings.getString / message.content）先 `.toRealString()` 再操作
2. 空白判断只用 `safeIsBlank()`；**禁止 `.isBlank()` / `.isNotBlank()` / `.isEmpty()`**——包括对"看起来已是真 String"的中间结果（如 `Regex.replace` 返回值），R8 跨函数内联仍会炸
3. 给 `LLMTranslator` 传配置时传原始 `getString(...)` 并 `as Any`

### 3.2 默认参数桥接（本项目编译环境特有）
`Regex.findAll(input)`、`Regex.find(input)`、`String.split(delimiter)` 等**依赖默认参数的调用在本项目编译环境下报 "No value passed for parameter 'p1'"**。必须显式传全部参数（`find(input, 0)`），或用手动循环/Java 原生 API（`indexOf`/`substring`）。已踩过三次（findAll、find、split），新增代码请自查。

### 3.3 Discord 126021 内部 API 脆弱点
- 自动翻译 hook `StoreStream.handleMessageCreate(api Message)`（`g()`=channelId、`o()`=messageId）
- 译文重渲染依赖 `DiscordParser.parseChannelMessage` + 反射 `getMessageRenderContext/getMessagePreprocessor/getSpoilerClickHandler`（已用 dex 验证签名），失败自动回退纯文本
- 频道模型没有公开 name 字段，设置子页面用频道 ID 展示

---

## 4. 已知问题 / 待办

- **设备实测未完成**：URL 点击、表情渲染、设置子页面、自动生成配置、停止自动翻译等均为运行时行为，CI 只保证编译
- 译文内存缓存 LRU 上限 300 条，长历史大幅滚动后旧译文可能被淘汰并重新翻译（持久缓存能兜住一部分，但批量路径的淘汰项会重新请求）
- LLM 服务商限流（429 rpm/TPM）仍可能导致批量降级 Google，完成 toast 会显示降级条数；`translate.log` 有 `[LLM] Batch attempt ... status` 可确认
- 批量行格式依赖模型遵守 `[id] 译文` 输出，个别模型可能不遵守（有 JSON 兼容解析兜底）
- `/translate` 命令不走批量、不带频道配置
- 频道配置目前只影响 LLM 后端，Google 后端忽略

---

## 5. 另一台设备无缝接手步骤

```bash
git clone https://github.com/kizfive/TranslateEnhanced.git
cd TranslateEnhanced
git checkout test          # 所有新功能在 test
git pull origin test
```

### 5.1 构建
- 本地：JDK 17 + Android SDK（compileSdk 31）后执行 `./gradlew :TranslateEnhanced:make generateUpdaterJson`
- 或直接用 CI Artifacts：每次 push 后到 Actions 运行页下载 `plugins` → 解压得到 `TranslateEnhanced.zip`
- 安装：`TranslateEnhanced.zip` 放入手机 `/sdcard/Aliucord/plugins/`，重启 Discord

### 5.2 设备实测清单（合并 main 前）
- [ ] 手动翻译带链接消息 → 译文里链接在、可点击
- [ ] 带 emoji/提及/自定义表情消息 → 译文里恢复显示
- [ ] 自动翻译：私聊开启 → 历史+新消息都被翻译；关闭 → 立即停止、不再降级
- [ ] 设置页：Discord 风格、LLM 配置、链接保护、翻译自己消息、清缓存
- [ ] 消息菜单：翻译/显示原文/重新翻译/频道翻译配置/自动翻译开关
- [ ] 频道配置：自动生成 → 自动保存 → 子页面能看到；翻译体现术语表
- [ ] 持久缓存：翻译后重启 Discord，同频道消息秒出译文；按频道清缓存生效
- [ ] 日志：Debug Mode 开启后 `/sdcard/Aliucord/translate.log` 有 DEBUG 记录

### 5.3 报障方式
让用户开 Debug Mode → 复现 → 取 `/sdcard/Aliucord/translate.log` 发回。日志里有：
- `handleMessageCreate: channel=... message=...`（新消息）
- `auto skip: ...`（自动翻译跳过原因）
- `auto batch: flushing N messages` / `[LLM] Batch attempt ... status`（批量请求）
- `cache hit` / `auto cache hit`（缓存命中）
- `auto dropped: channel disabled`（停止逻辑生效）

---

## 6. 设置项速查（Constants.kt）

| Key | 含义 | 默认 |
|---|---|---|
| `defaultLanguage` | 默认目标语言 | `zh-CN` |
| `cleanHtml` / `cleanUrl` / `cleanEmoji` | 清洗开关（URL=链接保护模式） | true / **false** / true |
| `backend` | google / llm | google |
| `llmBaseUrl` / `llmApiKey` / `llmModel` / `llmSystemPrompt` | LLM 配置 | 空 / 空 / gpt-4o-mini / 默认提示词 |
| `debugMode` | 调试日志 | false |
| `autoTranslateSelf` | 自动翻译自己的消息 | false |
| `channelPrompt_{id}` / `channelGlossary_{id}` | 频道提示词/术语表 | 空 |
| `channelConfigChannels` | 频道登记表（逗号分隔 ID） | 空 |

---

## 7. 下一步建议

1. 在设备上跑完 5.2 测试清单
2. 有问题继续在 `test` 上修（保持"push → CI → 盯到通过"的节奏）
3. 通过后：`git checkout main && git merge test && git push origin main`，CI 自动发布 `builds` 分支
